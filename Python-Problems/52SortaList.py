numbers = [5, 3, 8, 1, 2]

# Sort ascending
numbers.sort()
print("Sorted list:", numbers)
#The above code will aragnge in the normal sorting in order

# the below code was for the arrangeing the in reverse order
numbers.sort(reverse=True)
print("Descending order:", numbers)